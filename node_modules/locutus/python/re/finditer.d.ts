type ReFinditerFlags = number | string;
export type FinditerMatch = {
    match: string;
    index: number;
    groups: string[];
};
export declare function finditer(pattern: string | RegExp, source: string, flags?: ReFinditerFlags): FinditerMatch[];
export {};
