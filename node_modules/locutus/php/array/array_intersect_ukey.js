"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.array_intersect_ukey = array_intersect_ukey;
const _callbackResolver_ts_1 = require("../_helpers/_callbackResolver.js");
const _phpTypes_ts_1 = require("../_helpers/_phpTypes.js");
function array_intersect_ukey(arr1, ...arraysAndCallback) {
    //  discuss at: https://locutus.io/php/array_intersect_ukey/
    // original by: Brett Zamir (https://brett-zamir.me)
    //   example 1: var $array1 = {blue: 1, red: 2, green: 3, purple: 4}
    //   example 1: var $array2 = {green: 5, blue: 6, yellow: 7, cyan: 8}
    //   example 1: array_intersect_ukey ($array1, $array2, function (key1, key2){ return (key1 === key2 ? 0 : (key1 > key2 ? 1 : -1)); })
    //   returns 1: {blue: 1, green: 3}
    const retArr = {};
    const callback = arraysAndCallback.at(-1);
    if (typeof callback === 'undefined' || !(0, _phpTypes_ts_1.isPhpCallableDescriptor)(callback)) {
        throw new Error('array_intersect_ukey(): Invalid callback');
    }
    const arrays = arraysAndCallback.slice(0, -1).map((value) => (0, _phpTypes_ts_1.toPhpArrayObject)(value));
    const lastArrayIndex = arrays.length - 1;
    const keyComparator = (0, _callbackResolver_ts_1.resolveNumericComparator)(callback, 'array_intersect_ukey(): Invalid callback');
    arr1keys: for (const [k1, arr1Value] of (0, _phpTypes_ts_1.entriesOfPhpAssoc)(arr1)) {
        arrs: for (const [i, arr] of arrays.entries()) {
            for (const [k] of (0, _phpTypes_ts_1.entriesOfPhpAssoc)(arr)) {
                if (keyComparator(k, k1) === 0) {
                    if (i === lastArrayIndex) {
                        retArr[k1] = arr1Value;
                    }
                    // If the innermost loop always leads at least once to an equal value,
                    // continue the loop until done
                    continue arrs;
                }
            }
            // If it reaches here, it wasn't found in at least one array, so try next value
            continue arr1keys;
        }
    }
    return retArr;
}
