"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.array_uintersect_uassoc = array_uintersect_uassoc;
const _callbackResolver_ts_1 = require("../_helpers/_callbackResolver.js");
const _phpTypes_ts_1 = require("../_helpers/_phpTypes.js");
function array_uintersect_uassoc(arr1, ...arraysAndComparators) {
    //  discuss at: https://locutus.io/php/array_uintersect_uassoc/
    // original by: Brett Zamir (https://brett-zamir.me)
    //   example 1: var $array1 = {a: 'green', b: 'brown', c: 'blue', 0: 'red'}
    //   example 1: var $array2 = {a: 'GREEN', B: 'brown', 0: 'yellow', 1: 'red'}
    //   example 1: array_uintersect_uassoc($array1, $array2, function (f_string1, f_string2){var string1 = (f_string1+'').toLowerCase(); var string2 = (f_string2+'').toLowerCase(); if (string1 > string2) return 1; if (string1 === string2) return 0; return -1;}, function (f_string1, f_string2){var string1 = (f_string1+'').toLowerCase(); var string2 = (f_string2+'').toLowerCase(); if (string1 > string2) return 1; if (string1 === string2) return 0; return -1;})
    //   returns 1: {a: 'green', b: 'brown'}
    const retArr = {};
    const keyCallback = arraysAndComparators.at(-1);
    const valueCallback = arraysAndComparators.at(-2);
    if (typeof keyCallback === 'undefined' ||
        typeof valueCallback === 'undefined' ||
        !(0, _phpTypes_ts_1.isPhpCallableDescriptor)(keyCallback) ||
        !(0, _phpTypes_ts_1.isPhpCallableDescriptor)(valueCallback)) {
        throw new Error('array_uintersect_uassoc(): Invalid callback');
    }
    const arrays = arraysAndComparators.slice(0, -2).map((value) => (0, _phpTypes_ts_1.toPhpArrayObject)(value));
    const lastArrayIndex = arrays.length - 1;
    const keyComparator = (0, _callbackResolver_ts_1.resolveNumericComparator)(keyCallback, 'array_uintersect_uassoc(): Invalid key callback');
    const valueComparator = (0, _callbackResolver_ts_1.resolveNumericComparator)(valueCallback, 'array_uintersect_uassoc(): Invalid value callback');
    arr1keys: for (const [k1, arr1Value] of (0, _phpTypes_ts_1.entriesOfPhpAssoc)(arr1)) {
        arrs: for (const [i, arr] of arrays.entries()) {
            for (const [k, arrValue] of (0, _phpTypes_ts_1.entriesOfPhpAssoc)(arr)) {
                if (valueComparator(arrValue, arr1Value) === 0 && keyComparator(k, k1) === 0) {
                    if (i === lastArrayIndex) {
                        retArr[k1] = arr1Value;
                    }
                    // If the innermost loop always leads at least once to an equal value,
                    // continue the loop until done
                    continue arrs;
                }
            }
            // If it reaches here, it wasn't found in at least one array, so try next value
            continue arr1keys;
        }
    }
    return retArr;
}
